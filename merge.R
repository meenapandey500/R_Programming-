#Merging List : to merge or join or concatenate 2 or more list.
#Atleast 2 list must be occurs
#suppose we creates 2 list X and Y
X=list(45,89,90,56,67,87,40)
Y=list("Jan","Feb","Mar","Apr","May")
#merge the two list X and Y and hold new list Z
#for merge list use c( ) means combined function
Z=c(X,Y)
print("Merge List : ")
print(Z)