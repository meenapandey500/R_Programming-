#create a vector in R, which are as follows:
#1) Using the colon(:) operator
x<-5:-12 #bydefault decrement -1 (max : 5 to min : -12)
cat("Vector X : ",x)
#to show the datatype of vector x 
cat(typeof(x))
cat(class(x))